// https://www.youtube.com/watch?v=tyB0ztf0DNY 
 Introduction To Dynamic Programming :
Dynamic Programming can be described as storing answers to various sub-problems to be used later 
whenever required to solve the main problem.

The two common dynamic programming approaches are:

Memoization: (Top-Down) --> Recursion + storing already calculated results
============================================================================
-->Known as the “top-down” dynamic programming, usually the problem is solved in the 
direction of the main problem to the base cases.
--> Tend to store the value of subproblems in some Map or Table .

Tabulation:(Bottom-up) → Iterative approach + building results from base cases
================================================================================= 
-->Known as the “bottom-up '' dynamic programming, usually the problem is solved in the 
direction of solving the base cases to the main problem
--> Base case is required to solve in this approach.

Note: The base case does not always mean smaller input. 
It depends upon the implementation of the algorithm.

We will be using the example of Fibonacci numbers here. The following series is called the Fibonacci series:

0,1,1,2,3,5,8,13,21,...

We need to find the nth Fibonacci number, where n is based on a 0-based index. 
Every ith number of the series is equal to the sum of (i-1)th and (i-2)th number where 
the first and second number is given as 0 and 1 respectively.

Shortcut :
=================================
1. Try to represent the problem in terms of Indexes.
2. Do all possible stuffs on that index an according to the problem statement.
3. Sum of all stuffs -> if problem says --> 
						1. count all the ways or 
						2. find the minimum or 
						3. To find the maximum 